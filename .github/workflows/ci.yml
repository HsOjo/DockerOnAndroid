name: ci

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: syntax check (sh -n)
        run: |
          set -e
          for f in configure install.sh build-pasta.sh build-crun.sh uninstall.sh \
                   rootfs/usr/bin/podman rootfs/usr/bin/conmon \
                   rootfs/usr/libexec/podman/netavark \
                   rootfs/usr/local/bin/crun-nomq \
                   rootfs/usr/local/bin/doa-healthcheckd \
                   rootfs/etc/init.d/doa-cgroups \
                   rootfs/etc/init.d/doa-healthcheckd; do
            echo "> $f"
            sh -n "$f"
          done

      - name: shellcheck
        run: |
          sudo apt-get install -y shellcheck
          shellcheck -s sh \
            configure install.sh build-pasta.sh build-crun.sh uninstall.sh \
            rootfs/usr/bin/podman rootfs/usr/bin/conmon \
            rootfs/usr/libexec/podman/netavark \
            rootfs/usr/local/bin/crun-nomq \
            rootfs/usr/local/bin/doa-healthcheckd \
            rootfs/etc/init.d/doa-cgroups \
            rootfs/etc/init.d/doa-healthcheckd
